---
title: "POST /auth/refresh"
description: "Refresh access token"
---

## POST /auth/refresh
**Module:** `auth`

### What it does
🔴 Refresh access token

### Business Logic
🟡 The `refreshToken()` method, called by the `refresh()` handler, retrieves a new access token without making any direct database calls. It validates the provided refresh token and throws an `UnauthorizedException` if it is invalid. The method does not utilize Prisma operations.

### Request
| Param | Type | Source |
|-------|------|--------|
| `dto` | `RefreshTokenDto` | body |

### DTO Fields
**`RefreshTokenDto`**

| Field | Type | Required | Validators | Example |
|-------|------|----------|------------|---------|
| `refreshToken` | `string` | Yes | IsString, IsNotEmpty | eyJhbGciOiJIUzI1NiIs... |

### Responses
| Status | Description |
|--------|-------------|
| 200 | Token refreshed |
| 401 | Invalid refresh token |

### Execution Flow
`refresh()` → `refreshToken()`

### Service Details
**`refreshToken()`**
- Throws: `UnauthorizedException`
